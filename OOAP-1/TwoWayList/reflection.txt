- Я не определил конструктор в ParentList, т.к. абстрактный класс не может создавать свои экземпляры (вдобавок я описываю конструкторы как статические методы, а их нельзя переопределить у потомков).
- В моем решении по-прежнему нет запроса статуса для запроса Get, так как у меня в случае неудачи метод Get бросает ошибку (все из-за того, что сигнатура не допускает возврат nullable-значения).