1) ошибка компиляции, т.к. мы вызываем метод makeSound на объекте класса Animal, который больше не содержит такого метода.

2) ошибка компиляции, т.к. мы переопределям метод makeSound(int numberOfSounds) при помощи @Override, но родительский класс не содержит метода с такой сигнатурой. По крайней мере, так это работает в C#.

3) ошибка компиляции, т.к. вызов objectMapper.writerWithDefaultPrettyPrinter().writeValueAsString(result) использует переменную result, находящуюся вне области видимости.

Не очень понимаю, при чем здесь скрытые механизмы логики, ошибки компиляции это буквально самый явный тип ошибкок из всех возможных.